// Package series provides a generic, type-tagged column (Series[T]) and rich operations
// to manipulate and analyze slices as tabular columns (map, filter, reduce, aggregations,
// boolean/order statements, etc.). It is the core building block for the df package.
package series
